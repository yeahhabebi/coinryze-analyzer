coinryze-analyzer/
│
├─ frontend/               # your existing frontend
│   └─ coinryze_history.csv
├─ fetcher/
│   └─ fetch_coinryze.py   # the script we just rewrote
├─ requirements.txt
└─ render-worker.sh        # startup script for Render Worker
